# Copy to .env.github and set your GitHub PAT (repo scope).
# Get a token: https://github.com/settings/tokens → Generate new (classic) → scope: repo
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
