// Re-export entity types for DB layer (use shared in app code)
export type { User, Workspace, Source, Topic, TrendItem, DraftPost, ApprovedPost, ScheduleJob, PublishAttempt, Credential, PromptTemplate, PolicyRule, AuditEvent } from 'shared';
